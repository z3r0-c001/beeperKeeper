# BEEPER KEEPER 10000 - Environment Configuration
# Copy this file to .env and customize with your settings

# === Grafana SMTP / Email Configuration ===
# Enable email alerts (set to true to activate)
GF_SMTP_ENABLED=false

# SMTP Server Settings
GF_SMTP_HOST=smtp.gmail.com:587
GF_SMTP_USER=your-email@gmail.com
GF_SMTP_PASSWORD=your-16-char-app-password

# Email From Address
GF_SMTP_FROM_ADDRESS=grafana@beeper.local
GF_SMTP_FROM_NAME=Beeper Keeper Alerts

# Skip TLS verification (set to false for production)
GF_SMTP_SKIP_VERIFY=true

# === Alert Recipient ===
# Email address to receive alerts
ALERT_EMAIL=alerts@example.com

# === Instructions ===
# 1. Copy this file: cp .env.example .env
# 2. Edit .env with your actual credentials
# 3. For Gmail: Use App Password (https://myaccount.google.com/apppasswords)
# 4. Restart Grafana: docker-compose down && docker-compose up -d
# 5. Test alerts in Grafana UI: Alerting → Contact points → Test

# See ALERT_SETUP.md for detailed configuration guide
